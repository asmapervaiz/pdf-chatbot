# Optional: use OpenAI for embeddings (text-embedding-3-small) and/or chat (gpt-3.5-turbo).
# Leave empty to use local sentence-transformers + HuggingFace FLAN-T5.
OPENAI_API_KEY=

# Optional overrides (defaults are fine for most cases)
# MAX_UPLOAD_SIZE_MB=20
# CHUNK_SIZE=500
# TOP_K_CHUNKS=5
# EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2
# FALLBACK_MODEL=google/flan-t5-small
